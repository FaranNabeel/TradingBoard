# TradingBoard
This project is a server-client trading system built using Flask that enables a master account to replicate trades across multiple client accounts on the Bitget exchange. The system utilizes REST APIs to fetch balances, execute trades, and display real-time account statuses.
